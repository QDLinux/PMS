# 个人事务后台管理系统 — 软件安装及使用说明书

---

## 第一章 安装说明

### 1.1 硬件要求

运行本系统所需的硬件最低配置参见下表。

<div align="center"><b>表1-1 硬件要求</b></div>

| 硬件 | 最低要求 |
|------|---------|
| 计算机 | Intel 或兼容架构，Pentium 166 MHz 及以上 |
| 内存（RAM） | 不少于 4GB，建议 8GB 以上以获得流畅体验 |
| 硬盘空间 | 不少于 10GB 可用空间 |
| 监视器 | VGA 或更高分辨率，最低支持 800×600 |
| 指针设备 | Microsoft 鼠标或兼容设备 |
| CD-ROM 驱动器 | 不需要 |

> 上述为软件运行的底线配置。若操作系统本身占用较多资源，建议适当提高内存至 8GB。具体需求还会因所安装的工具链版本不同而有所浮动。

### 1.2 软件要求

下表列出了系统开发、部署及维护阶段所依赖的各项软件及其版本约束。

<div align="center"><b>表1-2 软件要求</b></div>

| 软件名称 | 软件版本 |
|---------|---------|
| IntelliJ IDEA | 2021.3.2 及以上版本 |
| JDK | 17+ |
| MySQL | 5.5+，推荐 5.7 |
| Maven | 3.6+ |
| Node.js | 16+ |
| npm | 8+ |

### 1.3 软件安装

#### 1.3.1 IntelliJ IDEA 的下载与安装

在浏览器地址栏输入 **https://www.jetbrains.com/idea/download/?section=windows**，进入 JetBrains 官方下载页。用户可根据需求选取 Community（社区版）或 Ultimate（旗舰版），点击 Download 按钮等待下载完成，如图 1-1 所示。

> **（图1-1 IDEA 下载界面）**

安装包获取后，双击运行，跟随安装向导逐步操作即可。在安装选项界面中，建议将"Add bin folder to the PATH"勾选上，其余选项保持默认。首次启动时可按提示选择 30 天试用或激活许可证。

#### 1.3.2 Node.js 的下载与安装

访问 **https://nodejs.org/en/download**，页面会自动推荐适合当前操作系统的 LTS 版本（18.x 或 20.x 均可），选择 Windows Installer（.msi）格式下载，参考图 1-2。

> **（图1-2 Node.js 下载界面）**

获取安装文件后，双击启动安装向导。除选择安装目录外，其他步骤保持默认即可。安装过程中可以开启"Automatically install the necessary tools"选项，便于后续编译原生模块。

装完之后，通过 Win + R 打开运行窗口输入 `cmd` 启动命令行，依次执行下方两条指令，若均能回显版本号，则表示环境已就绪：

```powershell
node -v
npm -v
```

若终端输出类似 `v18.x.x` 和 `9.x.x` 的信息，即代表安装无误，如图 1-3。

> **（图1-3 Node.js 版本验证）**

#### 1.3.3 JDK 的安装与配置

本系统后端基于 Java 17 构建，请确保安装 17 或更新版本的 JDK。

访问 **https://www.oracle.com/java/technologies/downloads/#java17** 进入官方下载页面，按操作系统筛选，Windows 平台选择 x64 Installer 安装包。下载完毕后双击运行，指定安装路径后完成安装。

JDK 安装之后还需要配置系统环境变量，具体步骤如下：

1. 在桌面右键"此电脑"，依次进入"属性" → "高级系统设置"
2. 在弹出的系统属性对话框中点击"环境变量"
3. 在系统变量区域点击"新建"，添加一条变量：
   - **变量名**：`JAVA_HOME`
   - **变量值**：填写 JDK 的安装根目录（例如 `C:\Program Files\Java\jdk-17`）
4. 找到系统变量中的 `Path` 条目，双击编辑，追加一条 `%JAVA_HOME%\bin`

> **（图1-4 系统变量配置）**
> **（图1-5 环境变量 Path 配置）**

变量写入完毕后，打开命令行运行以下命令进行校验：

```powershell
java -version
```

终端若能正确打印 Java 17 的版本详情，说明 JDK 已配置完毕，参考图 1-6。

> **（图1-6 JDK 版本验证）**

#### 1.3.4 Maven 的下载与配置

项目根目录自带了 Maven Wrapper（`mvnw.cmd`），不安装独立 Maven 也能正常构建。但如果希望在全局任意位置使用 `mvn` 指令，则可按以下流程单独部署。

浏览器打开 **https://maven.apache.org/download.cgi**，在 Files 区域下载 Binary zip 格式的压缩包，参阅图 1-7。

> **（图1-7 Maven 版本下载界面）**

将下载的 zip 包解压至自定义目录（例如 `D:\apache-maven-3.9.x`），然后配置系统环境变量：

1. 新增系统变量 `MAVEN_HOME`，取值指向解压路径
2. 在 `Path` 变量中追加 `%MAVEN_HOME%\bin`

执行 `mvn -version` 验证，若终端回显 Maven 与 Java 版本信息，说明安装无误。后续使用中如果没有手动安装 Maven，直接用项目中的 `mvnw.cmd` 替代 `mvn` 即可。

#### 1.3.5 MySQL 数据库的下载与安装

在浏览器中访问 **https://downloads.mysql.com/archives/installer/**，筛选 Product Version 为 5.7 系列（如 5.7.44），下载 MySQL Installer 包。

> **说明**：本系统兼容 MySQL 5.5 及以上版本，综合考虑稳定性推荐 5.7。若选用 MySQL 8.x，连接驱动层面无问题，但 DATETIME 数据类型在 8.0 中的行为略有差异，需关注兼容性。

获取安装包后双击执行，安装过程注意以下几点：

1. 安装类型选择"Developer Default"或"Server only"
2. 在账户设置环节，为 root 用户指定密码（建议与项目配置保持一致，设为 `root`）
3. 后续配置项一律使用默认值即可

安装完成后，在命令行输入以下命令连接数据库：

```powershell
mysql -uroot -p
```

输入此前设置的密码进入 MySQL 交互终端后，执行 `status;` 查看服务器版本详情，如图 1-8。

> **（图1-8 MySQL 版本验证）**

---

## 第二章 系统运行与使用

### 2.1 系统的初始化与运行

#### 2.1.1 导入项目到 IDEA

1. 打开 IntelliJ IDEA，在欢迎界面点击"Open"
2. 在弹出的资源管理器对话框中，定位到项目根文件夹（`aitest`）
3. 确认选择后，IDEA 会自动识别 pom.xml 并作为 Maven 工程导入
4. 右下角进度条走完后，即表示依赖下载与索引构建完成，参考图 2-1

> **（图2-1 IDEA 导入项目界面）**

#### 2.1.2 创建数据库并初始化

在运行项目之前，需要先在 MySQL 实例中建库并导入表结构。

**第一步：建立数据库**

通过命令行或数据库客户端（Navicat、DBeaver 等）连接到 MySQL，执行建库语句：

```sql
CREATE DATABASE personal_manager DEFAULT CHARACTER SET utf8;
```

**第二步：执行初始化脚本**

数据库初始化脚本位于 `src/main/resources/db/init_mysql.sql`，脚本内容包括四张业务表（sys_user、accounting_record、plan_item、goal）的 DDL 语句、索引以及一条默认管理员记录。

在项目根目录下打开命令行，运行下述导入命令：

```powershell
mysql -uroot -p personal_manager < src\main\resources\db\init_mysql.sql
```

> 若使用的是 MySQL 5.5 版本，导入完上述脚本后还需额外执行 `fix_mysql55_datetime.sql`，该脚本用于修复高版本 DATETIME 字段在 5.5 中的兼容问题。

**第三步：调整数据源配置**

编辑 `src/main/resources/application.yml`，将数据库连接的用户名与密码改为本机实际值：

```yaml
spring:
  datasource:
    username: root           # 替换为实际的数据库用户名
    password: root           # 替换为实际的数据库密码
```

#### 2.1.3 构建前端页面

前端工程位于 `frontend/` 目录下，技术选型为 Vue 3 + Vite。为保证后端启动后可直接访问控制台，建议先执行一次前端构建。

```powershell
cd frontend
npm install           # 仅首次执行，安装项目依赖
npm run build         # 编译打包，产物输出至 src/main/resources/static/app/
```

构建结束后，`src/main/resources/static/app/` 路径下应当出现 `index.html` 及配套的 JS/CSS 静态文件。

#### 2.1.4 启动后端服务

在 IDEA 中，导航至 `src/main/java/com/example/personalmanager/PersonalManagerApplication.java`，在类名上右键选择"Run 'PersonalManagerApplication'"启动，如图 2-2。

> **（图2-2 IDEA 启动主类界面）**

如果偏好命令行方式，在项目根路径下执行：

```powershell
.\mvnw.cmd spring-boot:run
```

日志中出现以下提示即表示服务已正常拉起：

```
Started PersonalManagerApplication in X.XXX seconds
```

应用默认监听 **8080** 端口。浏览器访问 **http://localhost:8080** 应能看到登录页，如图 2-3。

> **（图2-3 系统登录页面）**

#### 2.1.5 完整部署流程（一键操作）

如需将前端构建和后端启动合并为一次操作，可按顺序运行：

```powershell
cd frontend
npm install
npm run build
cd ..
.\mvnw.cmd spring-boot:run
```

### 2.2 系统的使用

#### 2.2.1 用户注册与登录

系统启动后浏览器打开 `http://localhost:8080` 即进入登录界面。进入系统有两种途径：

**途径一：直接使用预置管理员**

数据库初始化阶段已写入一条管理员记录，用户名密码如下：

| 字段 | 值 |
|------|-----|
| 用户名 | `admin` |
| 密码 | `admin123456` |

**途径二：自助注册**

在登录页面底部点击"注册账号"超链接，跳转到注册表单页面。填入用户名与密码后点击注册，成功后页面自动切回登录页，此时可用新凭证登录，参考图 2-4。

> **（图2-4 用户注册页面）**

认证通过后，页面将重定向至控制台总览面板，参考图 2-5。

> **（图2-5 登录成功后的控制台主页）**

#### 2.2.2 总览看板模块

总览看板作为系统的首屏页面，用卡片布局汇总了当前用户的几项核心数据：

- **本月收入**：当月全部收入类记录的累计金额
- **本月支出**：当月全部支出类记录的累计金额
- **本月结余**：收入与支出的差值
- **计划统计**：计划条目总数与已完结数量
- **目标统计**：目标条目总数与已达成数量

用户登录即进入此页面，可快速掌握最近一段时间的整体情况，如图 2-6。

> **（图2-6 总览看板界面）**

#### 2.2.3 记账管理模块

记账模块负责管理个人每一笔收支流水。点击左侧导航栏"记账管理"，其下划分了三个页签：

**记账总览**：罗列当前用户的所有交易记录，每条记录包含收支类型、金额、分类、日期以及备注。在该页面可进行以下操作（如图 2-7）：

- **新建一笔记录**：点击"新增记录"，在弹窗中选择交易方向（INCOME 收入 / EXPENSE 支出）、分类（工资、奖金、投资、餐饮、交通、购物、其他），填写金额、选定日期并输入备注后提交
- **修改已有记录**：点击某条记录行末尾的编辑图标，在弹出的表单中调整内容后保存
- **移除记录**：点击删除图标，确认后该条记录将被永久删除

> **（图2-7 记账总览界面）**

**月度账单**：按自然月汇总展示，便于按月对账，如图 2-8。

> **（图2-8 月度账单界面）**

**年度账单**：按自然年汇总展示，适合年底复盘，如图 2-9。

> **（图2-9 年度账单界面）**

#### 2.2.4 计划管理模块

计划管理模块用于规划和跟踪个人的待办事项。在左侧导航中选择"规划管理"，包含三级视图：

**规划总览**：以列表形式呈现全部计划，每条计划标有标题、描述、起止日期、紧急程度（低/中/高）和当前状态（进行中/已完成）。页面支持：

- **新建计划**：点击"新增计划"，填写标题、描述、起止日期，并从下拉菜单中选定优先级后保存
- **编辑计划**：对已有条目进行信息修正
- **删除计划**：清理不再需要的任务项
- **标记完成**：将某条计划从"进行中"扭转为"已完成"

规划总览页面参考图 2-10。

> **（图2-10 规划总览界面）**

**月度规划**：按时间维度筛选出某一月份的计划，便于集中审视当月安排，如图 2-11。

> **（图2-11 月度规划界面）**

**年度规划**：同理按年份做筛选汇总，适合制定年度回顾，如图 2-12。

> **（图2-12 年度规划界面）**

#### 2.2.5 目标管理模块

目标模块面向储蓄、学习等需要量化追踪的场景，支持设定目标金额并随时更新进度。在左侧导航中进入"目标管理"，下设三级视图：

**目标总览**：以列表+环形进度条的形式展示每个目标，字段包含名称、描述、目标金额、当前金额、起止日期以及执行状态（进行中/已完成/已取消）。在此页面可执行：

- **新建目标**：点击"新增目标"，输入目标名称、描述、目标金额、起止日期后提交
- **编辑目标**：调整目标各项参数
- **删除目标**：移除不需要继续追踪的目标
- **更新进度**：点击"更新进度"，输入增量金额，系统自动累加到当前金额并刷新进度条；一旦当前金额达到目标金额，状态会自动切换为"已完成"

目标总览页面参考图 2-13。

> **（图2-13 目标总览界面）**

**月度目标**：按月份维度过滤展示，参考图 2-14。

> **（图2-14 月度目标界面）**

**年度目标**：按年份维度过滤展示，参考图 2-15。

> **（图2-15 年度目标界面）**

#### 2.2.6 AI 智能助手模块

AI 助手对接 DeepSeek 大模型，通过 Function Calling 机制实时查询用户数据并生成分析结论。使用前请确保 `application.yml` 中 `app.ai.api-key` 已填入有效的 API Key。左侧导航进入"AI 助手"，包含三个视图：

**智能对话**：采用多轮对话模式，用户可用自然语言提问，AI 会先调用系统内部接口拉取真实数据再给出答复。典型提问如：
- "这个月我花了多少钱？"
- "我的目标进度怎么样？"
- "帮我分析一下本月的收支情况"

系统支持多个独立会话并存，用户可随时新建或切换。AI 回复的下层内容是 Markdown 原文，前端已对其做了排版渲染，对话页面如图 2-16。

> **（图2-16 智能对话界面）**

**理财建议**：AI 依据用户过往的记账记录，对消费结构做汇总分析并输出个性化建议，如图 2-17。

> **（图2-17 理财建议界面）**

**智能提醒**：AI 会扫描用户当前计划与目标的进度，针对滞后的条目生成推进提醒，如图 2-18。

> **（图2-18 智能提醒界面）**

#### 2.2.7 账号管理模块

账号管理板块允许用户对自身资料进行日常维护。进入左侧"账号管理"后，可进行如下操作：

- **更新昵称**：单击昵称旁的编辑图标，键入新值并确认
- **更换用户名**：修改登录用户名（不可与其他用户重复）
- **绑定邮箱**：填写或更新邮箱地址
- **更换头像**：点击头像占位区，从本地选取图片上传
- **重置密码**：输入旧密码与新密码，二次确认后生效

账号管理界面参考图 2-19。

> **（图2-19 账号管理界面）**

#### 2.2.8 退出登录

单击侧边栏最下方的"退出登录"，浏览器将清除本地缓存的 JWT 凭证并返回至登录页面。

---

## 第三章 系统技术指标

### 3.1 后端技术栈

| 技术 | 版本/说明 |
|------|----------|
| 运行环境 | Java 17 |
| 核心框架 | Spring Boot 3.3.8 |
| 安全框架 | Spring Security + JWT (jjwt 0.12.6) |
| 持久层框架 | Spring Data JPA + Hibernate |
| 数据库 | MySQL 5.5+ |
| 构建工具 | Maven 3.6+（项目内置 Maven Wrapper） |

### 3.2 前端技术栈

| 技术 | 版本/说明 |
|------|----------|
| UI 框架 | Vue 3.5.13（纯手写组件，无第三方组件库） |
| 构建工具 | Vite 5.4.10 |
| 语言 | JavaScript ES Module |
| 运行时 | Node.js 16+ |

### 3.3 AI 接口规格

| 项目 | 说明 |
|------|------|
| 接口协议 | OpenAI 兼容 Chat Completions API |
| 默认模型 | DeepSeek Chat |
| 对话模式 | 多轮对话 + Tool Calling |
| 可调用工具 | 查询记账记录、月度收支汇总、计划列表、目标状态等 |

---

## 第四章 常见问题与故障排除

### 4.1 后端启动失败

**故障现象**：启动过程中抛出 `CommunicationsException: Communications link failure`

**排查思路**：确认 MySQL 服务处于运行状态；检查 `application.yml` 中填写的数据库地址、端口、用户名和密码是否与本地环境匹配。

**故障现象**：日志出现 `Table 'personal_manager.sys_user' doesn't exist`

**排查思路**：数据库表尚未建立，请按照 2.1.2 节步骤执行初始化脚本。

**故障现象**：`Port 8080 was already in use`

**排查思路**：端口被占用。可在 `application.yml` 中将 `server.port` 调整为其他未使用的端口（如 9090），同时不要忘记修改 `frontend/vite.config.js` 中 proxy 的 target 地址。

### 4.2 前端构建失败

**故障现象**：`npm install` 长时间无进度或报网络超时错误

**排查思路**：npm 默认源在境内访问较慢，可切换至国内镜像：

```powershell
npm config set registry https://registry.npmmirror.com
npm install
```

切换源之后重新执行安装即可。

### 4.3 登录后页面空白

**故障现象**：输入用户名密码登录成功，但跳转后页面没有任何内容显示

**排查思路**：前端静态资源尚未构建。进入 `frontend/` 目录执行 `npm run build`，构建完成后重启后端再试。

### 4.4 AI 助手无响应

**故障现象**：向 AI 发送消息后长时间无反馈，或直接返回错误

**排查思路**：
1. 先确认 `application.yml` 中 `app.ai.enabled` 是否为 `true`
2. 再检查 `app.ai.api-key` 是否填写了有效的密钥
3. 确认本机网络能正常访问 `https://api.deepseek.com`
4. 如以上均无问题，请查看 `logs/ai-chat/` 目录下的日志文件，其中记录了每次请求的详细错误信息

### 4.5 页面样式异常

**故障现象**：控制台界面出现布局错位或部分样式丢失

**排查思路**：先尝试清除浏览器缓存后重新加载页面。若问题依旧，请确认前端是否已执行过最新一次构建（`npm run build`），旧版本的静态资源可能导致样式与逻辑不匹配。
